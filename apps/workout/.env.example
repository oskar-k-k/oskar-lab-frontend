AUTH_PLATFORM_URL=http://localhost:10030
AUTH_BACKEND_URL=http://127.0.0.1:10081
# Set only if the public reverse-proxy origin differs from AUTH_PLATFORM_URL.
# WORKOUT_PUBLIC_URL=https://lab.example.com
# Same server-only bridge secret as the platform and backend. Never use NEXT_PUBLIC.
AUTH_BRIDGE_SECRET=
